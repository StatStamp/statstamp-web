'use client';

// This page redirects to /participants/teams/[id]/edit — this component is never rendered.
// Kept as a stub to avoid TypeScript errors from the old implementation.
interface Props {
  id: string;
}

export function EditTeamContent({ id: _id }: Props) {
  return null;
}
