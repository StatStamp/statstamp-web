'use client';

// This page redirects to /participants/teams/new — this component is never rendered.
// Kept as a stub to avoid TypeScript errors from the old implementation.
export function NewTeamContent() {
  return null;
}
